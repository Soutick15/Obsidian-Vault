# Day 10 — Cost Management & FinOps: Caching + Cost Accounting
#
# All core tasks use Python stdlib only.
# scikit-learn is optional — used only in the BONUS semantic cache variant
# that computes TF-IDF cosine similarity instead of Jaccard.
#
# The shared service (labs/devops/_shared/) requires:
#   pip install -r labs/devops/_shared/requirements.txt
#
# Optional:
# scikit-learn>=1.3      # bonus TF-IDF semantic cache
# anthropic>=0.30        # real LLM path (set ANTHROPIC_API_KEY)
# openai>=1.0            # real LLM path (set OPENAI_API_KEY)
